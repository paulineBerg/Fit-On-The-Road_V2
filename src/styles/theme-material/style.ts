import { ThemeOptions } from "@mui/material";

// eslint-disable-next-line import/prefer-default-export
export const style: ThemeOptions = {
  palette: {
    mode: "dark",
    primary: {
      main: "#ed130d",
    },
    secondary: {
      main: "#8b725d",
    },
    background: {
      default: "#151311",
      paper: "#221f1d",
    },
  },
};
